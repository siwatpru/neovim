# Ghostscript (PDF/image ops)
brew install ghostscript

# Lightweight LaTeX engine (pick ONE: tectonic is tiny; full MacTeX is huge)
brew install tectonic
# (Alternative) brew install --cask mactex # ~5GB, includes pdflatex

# Mermaid CLI (mmdc)
brew install node # if you don’t already have Node
npm install -g @mermaid-js/mermaid-cli

# (Optional but often useful) ImageMagick for conversions
brew install imagemagick
